{
  "name": "donate-contract",
  "version": "1.0.0",
  "description": "a simple smart contract to donate",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "npx hardhat compile",
    "local:node": "npx hardhat node",
    "deploy:local": "npx hardhat run --network localhost scripts/01-deploy-donate.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "eslint": "^8.48.0",
    "hardhat": "^2.17.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
